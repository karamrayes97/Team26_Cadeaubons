<Window x:Class="Cadeaubons_Presentation.Windows.ThemeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cadeaubons_Presentation.Windows"
        mc:Ignorable="d"
        Title="Alle thema's" Height="450" Width="800"
        Background="White"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">

            <Button Content="← Terug"
                    Style="{StaticResource SecondaryButton}"
                    DockPanel.Dock="Right"
                    Width="100"
                    Click="BtnBack_Click"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="🎨" FontSize="28" Margin="0,0,10,0"/>
                <TextBlock Text="Alle thema's" Style="{StaticResource WindowTitle}" VerticalAlignment="Center"/>
            </StackPanel>

        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Center" Width="350" Margin="0,20,0,20">

                <ListBox x:Name="ThemeList"
                         SelectionChanged="ThemeList_SelectionChanged"
                         BorderThickness="0">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F5F5F5"
                                    CornerRadius="8"
                                    Padding="10"
                                    Margin="0,0,0,10">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Window>