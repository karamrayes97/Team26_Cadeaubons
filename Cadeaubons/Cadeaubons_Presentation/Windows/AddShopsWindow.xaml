<Window x:Class="Cadeaubons_Presentation.Windows.AddShopsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cadeaubons_Presentation.Windows"
        mc:Ignorable="d"
        Title="Winkel toevoegen" Height="600" Width="800"
        Background="White"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0">
            <Button Content="← Terug"
                    Style="{StaticResource SecondaryButton}"
                    DockPanel.Dock="Right"
                    Width="100"
                    Click="BtnBack_Click"/>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🏪" FontSize="28" Margin="0,0,10,0"/>
                <TextBlock Text="Winkel toevoegen"
                           Style="{StaticResource WindowTitle}"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </DockPanel>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Width="350"
                        Margin="0,20">

                <!-- Naam -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="🏪" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Naam winkel *" Style="{StaticResource FieldLabel}"/>
                </StackPanel>
                <TextBox x:Name="StoreNameTextBox"
                         Style="{StaticResource RoundedTextBox}"
                         Margin="0,0,0,15"/>

                <!-- Adres -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="📍" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Adres winkel *" Style="{StaticResource FieldLabel}"/>
                </StackPanel>
                <TextBox x:Name="StoreAdressTextBox"
                         Style="{StaticResource RoundedTextBox}"
                         Margin="0,0,0,15"/>

                <!-- Telefoonnummer -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="📞" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Telefoonnummer *" Style="{StaticResource FieldLabel}"/>
                </StackPanel>
                <TextBox x:Name="StoreNumberTextBox"
                         Style="{StaticResource RoundedTextBox}"
                         Margin="0,0,0,15"/>

                <!-- Stad -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="🌆" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Stad *" Style="{StaticResource FieldLabel}"/>
                </StackPanel>
                <ComboBox x:Name="CitiesListComboBox"
                          Height="38"
                          Margin="0,0,0,25"/>

                <!-- Toevoegen -->
                <Button Content="Winkel toevoegen"
                        Style="{StaticResource PrimaryButton}"
                        Margin="0,0,0,10"
                        Click="Button_Click"/>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Window>